<% content_for(:page_title, "Editing #{@item.title}") %>

<h1><%= app_title %></h1>

<nav>
  <ul>
    <li><%= link_to 'Item', lending_show_path(directory: @item.directory) %></li>
  </ul>
</nav>

<%= render(partial: 'form', locals: { item: @item }) %>
