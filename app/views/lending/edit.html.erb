<% content_for(:page_title, "Editing #{@item.title}") %>

<h1><%= page_header %></h1>

<nav>
  <ul>
    <li><%= link_to 'Admin view', lending_show_path(directory: @item.directory) %></li>
  </ul>
</nav>

<%= render(partial: 'form', locals: { item: @item }) %>
